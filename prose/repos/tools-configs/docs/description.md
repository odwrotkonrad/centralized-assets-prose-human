Dotfiles extended into root OS space. One `root/` tree loads onto the host: symlinked by default, `.ontoHost.cp` copied, `*.ontoHost.tpl` rendered. Repo docs, the license, and the AI prose payloads render from `centralized/assets/prose/*` at pinned versions. Only the local `.env` seed renders from a repo-local `*.ontoRepo.tpl`.
